# used-car-data-preprocessing-day12
Colab notebook performing a complete preprocessing pipeline on a used car resale dataset. Detects and caps outliers with the IQR method, applies ordinal and one-hot encoding, splits data into train/test sets, and standardizes features—fitting all transformations only on training data to avoid leakage.
